const INITIAL_STATE = {
  player: {
    name: '',
    assertions: '',
    score: 0,
    gravatarEmail: '',
  },
};

export const reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  // desenvolver cases para cada action

  default:
    return state;
  }
};
